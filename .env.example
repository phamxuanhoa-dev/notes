# Môi trường Flask: development hoặc production
FLASK_ENV=development

# Secret key cho ứng dụng, dùng để ký session, cookie, v.v.
# Hãy thay đổi thành một chuỗi ngẫu nhiên, dài và an toàn trong môi trường production.
SECRET_KEY="your-super-secret-and-long-random-string"
JWT_SECRET_KEY="your-other-super-secret-and-long-random-string"

# URL kết nối database.
# Cho SQLite (phát triển):
DATABASE_URL="sqlite:///../instance/dev.db"
# Cho PostgreSQL (production):
# DATABASE_URL="postgresql://user:password@hostname:port/database_name"

# Cấu hình CORS: danh sách các origin được phép, phân tách bằng dấu phẩy
CORS_ORIGINS="http://localhost:5173,http://127.0.0.1:5173"